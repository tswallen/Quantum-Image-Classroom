import React from 'react';
import ReactDOM from 'react-dom/client';
import MinigameCarousel from '../minigames/MinigameCarousel';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MinigameCarousel />
  </React.StrictMode>
);
